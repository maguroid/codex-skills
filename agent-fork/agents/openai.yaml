interface:
  display_name: "Agent Fork"
  short_description: "Fork the current context into a background agent"
  default_prompt: "Use $agent-fork to review the current work in parallel."
policy:
  allow_implicit_invocation: false
